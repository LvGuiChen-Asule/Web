<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.visitor.modules.system.mapper.SysRoleMapper">

    <select id="selectRolesByUserId" resultType="com.campus.visitor.modules.system.entity.SysRole">
        SELECT r.*
        FROM sys_role r
        LEFT JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="selectRoleCodesByUserIds" resultType="com.campus.visitor.modules.system.dto.admin.UserRolePair">
        SELECT ur.user_id as userId, r.role_code as roleCode
        FROM sys_user_role ur
        JOIN sys_role r ON ur.role_id = r.id
        WHERE ur.user_id IN
        <foreach collection="userIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="selectByRoleCodes" resultType="com.campus.visitor.modules.system.entity.SysRole">
        SELECT *
        FROM sys_role
        WHERE role_code IN
        <foreach collection="roleCodes" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
    </select>

</mapper>
